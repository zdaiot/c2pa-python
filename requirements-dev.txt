# Build dependencies
wheel==0.41.2  # For building wheels
setuptools==68.0.0  # For building packages
build==1.0.3  # For building packages using PEP 517/518
toml==0.10.2  # For reading pyproject.toml files

# Testing dependencies
pytest>=8.1.0
pytest-benchmark>=4.0.0,<5.0.0

# for downloading the library artifacts
requests>=2.0.0

# Code formatting
autopep8==1.7.0  # For automatic code formatting (compatible with flake8 5.0.4's pycodestyle)
flake8==5.0.4

# Test dependencies (for callback signers)
cryptography>=45.0.6
 
# Documentation (disabled for Python 3.8 compatibility - these packages have dependency conflicts)
# Sphinx>=5.0.0,<7.0.0
# sphinx-autoapi>=2.0.0,<3.0.0
# myst-parser>=1.0.0,<2.0.0
# furo>=2023.0.0,<2024.0.0